<%= form_for(@tipocarga) do |f| %>
  <% if @tipocarga.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipocarga.errors.count, "error") %> prohibited this tipocarga from being saved:</h2>

      <ul>
      <% @tipocarga.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo %><br />
    <%= f.text_field :tipo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
