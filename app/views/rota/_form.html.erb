<%= javascript_include_tag "teste.js" %>
<%= form_for(@rotum) do |f| %>
  <% if @rotum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rotum.errors.count, "error") %> prohibited this rotum from being saved:</h2>

      <ul>
      <% @rotum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  <br><%= f.text_field :origem, :class => 'inputtext2 text_field DOMControl_placeholder', :placeholder => "Origem", :fieldCls => 'search-field' %><p>
  </div>
  <div class="field">
  <%= f.text_field :destino, :class => 'inputtext2 text_field DOMControl_placeholder', :placeholder => "Destino" %><p>
  </div>
  <div class="field">
    <%= f.label :data %>
    <%= f.date_select :data %>-
	  <%= f.label :horario %>
    <%= f.time_select :horario %>
  </div></br>
  <div class="field">
    <%= f.label :gmaps %><br />
    <%= f.check_box :gmaps %>
  </div>
  <div class="actions">
    <%= f.submit :Cadastrar, :class => 'i2Style' %>
  </div>
<% end %>
